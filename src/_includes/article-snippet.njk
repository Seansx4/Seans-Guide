<h3>{{ post.data.title }}</h3>
{# <p>{{ post.data.datum | postDate }}</p> #}
<p>{{ post.data.datum }}</p>
<p>{{ post.data.description }}</p>
<a href="{{ post.url }}">Continue Reading</a>
</div>
<img
class="feature__image"
src="{{ post.data.image1 }}"
alt="{{ post.data.imageAlt1 }}"
/>
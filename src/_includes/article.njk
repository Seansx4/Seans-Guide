---
layout: "base.njk"
---

<section class="block">
  <div class="container">
    <header class="block__header" data-aos="fade-up">
      <h2>{{ title }}</h2>
    </header>

    <article class="feature grid grid--1x2" data-aos="zoom-in-up">
      <div class="feature__content">
        <h3 class="feature__heading">{{ subheading1 }}</h3>

        {{ paragraph1 | safe }}
      </div>
      <img
        class="feature__image image--rounded"
        src="{{ image1 }}"
        alt="{{ imageAlt1 }}"
      />
    </article>

    {% if 'subheading2' in content %}

    <article class="feature">
      <div class="feature__content">
        <h3 class="feature__heading">{{ subheading2 }}</h3>

        {{ paragraph2 | safe }}
      </div>
    </article>

    {% endif %}
  </div>
</section>

<!-- <section class="block">
  <div class="container">
    <header class="block__header" data-aos="fade-up">
      <h2>{{ title }}</h2>
    </header>

    <article class="feature grid grid--1x2" data-aos="zoom-in-up">
      <div class="feature__content">
        <h3 class="feature__heading">{{ subheading1 }}</h3>

        {{ paragraph1 | safe }}
      </div>
      <img
        class="feature__image image--rounded"
        src="{{ image1 }}"
        alt="{{ imageAlt1 }}"
      />
    </article>

    {% if 'subheading2' in content %}

    <article class="feature grid grid--1x2" data-aos="zoom-in-up">
      <div class="feature__content">
        <h3 class="feature__heading">{{ subheading2 }}</h3>

        {{ paragraph2 | safe }}
      </div>
      <img
        class="feature__image image--rounded"
        src="{{ image2 }}"
        alt="{{ imageAlt2 }}"
      />
    </article>

    {% endif %}
  </div>

  <div>Please work</div>
</section> -->

<script src="js/main.js"></script>
<script src="https://unpkg.com/aos@next/dist/aos.js"></script>
<script>
  AOS.init();
</script>
